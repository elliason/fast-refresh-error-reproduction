import { Canvas, Controls, Description, Meta, Primary, Stories, Title } from "@storybook/blocks";
import * as TableStories from "./table.stories";

<Meta of={TableStories} name="Docs" />

<Title />
[How to create advanced data table](https://ui.shadcn.com/docs/components/data-table) ```jsx import {
  (Table, TableBody, TableCaption, TableCell, TableHead, TableHeader, TableRow)
} from "@project/ui/components/table" ```
<Controls />
<Stories />
