import { Canvas, Controls, Meta, Primary, Stories, Title } from "@storybook/blocks";
import * as AlertStories from "./alert.stories";

<Meta of={AlertStories} name="Docs" />

<Title />
```jsx import {(Alert, AlertTitle, AlertDescription)} from "@project/ui/components/alert"; ```
<Controls />
<Stories />
