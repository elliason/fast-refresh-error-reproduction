import { Canvas, Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as CommandStories from './command.stories';

<Meta of={CommandStories} name="Docs" />

<Title />
<Description />

[Learn how to use it with keyboard shortcut](https://ui.shadcn.com/docs/components/command)

```jsx
import {
    Command,
    CommandDialog,
    CommandEmpty,
    CommandGroup,
    CommandInput,
    CommandItem,
    CommandList,
    CommandSeparator,
    CommandShortcut,
} from '#components/command';
```

<Stories />
<Controls />
