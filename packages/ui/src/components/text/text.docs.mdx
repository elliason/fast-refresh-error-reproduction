import { Canvas, Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as TypographyStories from './text.stories';

<Meta of={TypographyStories} name="Docs" />

<Title />
```jsx import {(Text, Strong, Muted, Code, TextLink, EyebrowTitle)} from "@mf/ui/components/text"; ```
<Controls />
<Stories />
