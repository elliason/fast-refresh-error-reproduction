import { Canvas, Controls, Description, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as AvatarStories from './avatar.stories';

<Meta of={AvatarStories} name="Docs" />

<Title />
[RadixUI API references](https://www.radix-ui.com/primitives/docs/components/avatar) An image element with a fallback for
representing the user.

```jsx
import { Avatar, AvatarFallback, AvatarImage } from '#components/avatar';
```

<Controls />
<Stories />
