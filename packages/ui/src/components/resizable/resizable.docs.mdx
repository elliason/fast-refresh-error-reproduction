import { Canvas, Controls, Description, Meta, Primary, Stories, Title } from "@storybook/blocks";
import * as ResizableStories from "./resizable.stories";

<Meta of={ResizableStories} name="Docs" />

<Title />
<Description />
```jsx import {(ResizableHandle, ResizablePanel, ResizablePanelGroup)} from "@project/ui/components/resizable" ```
<Controls />
<Stories />
