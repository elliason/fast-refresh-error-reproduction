import * as CalendarStories from './calendar.stories';
import { Canvas, Meta, Title, Primary, Controls, Stories, Description } from '@storybook/blocks';

<Meta of={CalendarStories} name="Docs" />

<Title />
A component for date fields that enables users to input and modify dates.
<Primary />
<Controls />