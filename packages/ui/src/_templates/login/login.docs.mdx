import { Canvas, Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as LoginStories from './login.stories';

<Meta of={LoginStories} name="Docs" />

<Title />
```jsx import {Button} from '@mf/ui/components/button'; import {Checkbox} from '@mf/ui/components/checkbox'; import {
    (Field, Form)
} from '@mf/ui/components/form'; import {Input} from '@mf/ui/components/input'; import {Label} from '@mf/ui/components/label';
import {(Muted, Text, TextLink)} from '@mf/ui/components/text'; ```
<Stories />
