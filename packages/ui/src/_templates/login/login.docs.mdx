import { Canvas, Controls, Meta, Primary, Stories, Title } from "@storybook/blocks";
import * as LoginStories from "./login.stories";

<Meta of={LoginStories} name="Docs" />

<Title />
```jsx import {Button} from '@project/ui/components/button'; import {Checkbox} from '@project/ui/components/checkbox'; import {
  (Field, Form)
} from '@project/ui/components/form'; import {Input} from '@project/ui/components/input'; import {Label} from '@project/ui/components/label';
import {(Muted, Text, TextLink)} from '@project/ui/components/text'; ```
<Stories />
