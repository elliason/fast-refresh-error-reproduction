import { Canvas, Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as NotFoundStories from './not-found.stories';

<Meta of={NotFoundStories} name="Docs" />

<Title />
```jsx import {ArrowRight} from 'lucide-react'; import {Badge} from '@mf/ui/components/badge'; import {Button} from '@mf/ui/components/button';
```
<Stories />
