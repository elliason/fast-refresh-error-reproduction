import { Canvas, Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as VerticalNavigationStories from './vertical-navigation.stories';

<Meta of={VerticalNavigationStories} name="Docs" />

<Title />
```jsx
import { CalendarIcon, ChartPieIcon, FileIcon, FolderIcon, HomeIcon, UsersIcon } from 'lucide-react';
```
<Controls />
<Stories />
