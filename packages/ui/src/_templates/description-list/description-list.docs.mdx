import { Canvas, Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as DescriptionListStories from './description-list.stories';

<Meta of={DescriptionListStories} name="Docs" />

<Title />
```jsx import {Button} from '@mf/ui/components/button'; import {
    (Card, CardContent, CardDescription, CardHeader, CardTitle)
} from '@mf/ui/components/card'; import {(DefinitionDescription, DefinitionItem, DefinitionList, DefinitionTerm)} from '@mf/ui/components/definition-list';
import {(Muted, Text)} from '@mf/ui/components/text'; ```
<Stories />
