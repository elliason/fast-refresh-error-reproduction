import { Canvas, Controls, Meta, Primary, Stories, Title } from "@storybook/blocks";
import * as DescriptionListStories from "./description-list.stories";

<Meta of={DescriptionListStories} name="Docs" />

<Title />
```jsx import {Button} from '@project/ui/components/button'; import {
  (Card, CardContent, CardDescription, CardHeader, CardTitle)
} from '@project/ui/components/card'; import {(DefinitionDescription, DefinitionItem, DefinitionList, DefinitionTerm)} from
'@project/ui/components/definition-list'; import {(Muted, Text)} from '@project/ui/components/text'; ```
<Stories />
