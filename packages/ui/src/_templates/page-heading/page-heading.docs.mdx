import { Canvas, Controls, Meta, Primary, Stories, Title } from '@storybook/blocks';
import * as PageHeadingStories from './page-heading.stories';

<Meta of={PageHeadingStories} name="Docs" />

<Title />

```jsx
import { Banknote, Briefcase, CalendarDays, ChevronLeftIcon, ChevronRightIcon, MapPin } from 'lucide-react';
import { Button } from '../../button';
```

<Stories />
